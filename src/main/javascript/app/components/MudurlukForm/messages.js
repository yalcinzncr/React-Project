/*
 * MudurlukForm Messages
 *
 * This contains all the text for the MudurlukForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MudurlukForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MudurlukForm container!',
  },
});
