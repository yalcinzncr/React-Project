/*
 * SikayetTur Messages
 *
 * This contains all the text for the SikayetKonu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SikayetKonu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SikayetKonu container!',
  },
});
