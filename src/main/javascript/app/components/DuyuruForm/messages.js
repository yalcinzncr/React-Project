/*
 * DuyuruForm Messages
 *
 * This contains all the text for the DuyuruForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DuyuruForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DuyuruForm container!',
  },
});
