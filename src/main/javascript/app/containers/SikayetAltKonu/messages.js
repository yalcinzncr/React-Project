/*
 * SikayetAltKonu Messages
 *
 * This contains all the text for the SikayetAltKonu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SikayetAltKonu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SikayetAltKonu container!',
  },
});
