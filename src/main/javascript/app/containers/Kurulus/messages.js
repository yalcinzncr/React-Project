/*
 * Kurulus Messages
 *
 * This contains all the text for the Kurulus container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Kurulus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Kurulus container!',
  },
});
